import 'package:belajar_dart_library/hello.dart'; // pola = 'package' : name package / name lib file we want to use. // not recommended to use /src/ to import
void main(List<String> args) {
  print(sayHello('Anton'));
} 